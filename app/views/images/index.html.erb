<style>
  * {
    padding: 0;
    margin: 0;
  }
  .fit { /* set relative picture size */
    max-width: 100%;
    max-height: 100%;
  }
  .center {
    display: block;
    margin: auto;
  }
</style>

<div id="image_request">


<div class="container-fluid" style="margin-bottom: 25px;">
  <b style="font-size: 35px; padding-top: 45px;"><%= 'nema još slika...budi prvi ;)' if @image.blank? %></b>
  <% unless @image.blank? %>
    <div id="image_id"><%= @image.id %></div>
      <%= image_tag(@image.path_to_image, class: "img-rounded center fit" ,style: "height: 1080px; width: 1920px;") %>
  <% end %>
</div>

  <%= link_to @event do %>
      <i class="ion-arrow-left-b" style="font-size: 24px"> Back</i>
  <% end %>
<script>
    $(document).ready(
        function() {
            setInterval(function() {
                window.location.href = '/images/index?id='+gon.image_hash['event']+'&current_image='+gon.image_hash['current_image']+'&last_image='+gon.image_hash['last_image'];
            }, 5000);
        });

    function set_body_height() { // set body height = window height
        $('body').height($(window).height());
    }
    $(document).ready(function() {
        $(window).bind('resize', set_body_height);
        set_body_height();
    });
</script>
</div>